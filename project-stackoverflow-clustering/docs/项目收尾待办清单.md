# 项目收尾待办清单

本文档根据 `运维文本聚类项目实施方案.md`、`大数据分析技术项目-2026版.pdf` 和当前项目实现情况整理，用于提交前最后检查。

## 1. 当前总体状态

项目主体代码和结果已经基本完成：

- 已实现 StackOverflow Oracle 问答文本聚类与相似问题发现。
- 已包含 Hadoop/HDFS、Spark on YARN、Spark ML、EDA、主题聚类、LSH 相似问题发现、连通分量重复簇和命令行 Demo。
- 已生成全量 JSONL，行数为 152758。
- 已导出 EDA、主题聚类、相似问题对和重复问题簇结果。
- 已有 README、项目实施文档、完整实验报告和 PPT 大纲。

但若按课程提交要求和现场演示要求看，还需要完成以下收尾工作。

## 2. 必须补齐

### 2.1 制作正式 PPT 文件

收尾后已补充正式 PPT 文件：

```text
project-stackoverflow-clustering/docs/运维文本聚类项目汇报.pptx
```

`PPT大纲.md` 仍作为内容大纲保留。

建议 PPT 内容包括：

- 项目背景与目标。
- 数据集介绍。
- 集群环境。
- 系统架构。
- 数据预处理。
- EDA 结果。
- 文本特征工程。
- 主题聚类算法与结果。
- LSH 相似问题发现与重复簇结果。
- Demo 演示流程。
- 遇到的问题与解决方案。
- 小组分工。
- 总结与改进方向。

### 2.2 重新确认集群现场可用

报告中已经写明集群和 SparkPi 验证成功，但提交或答辩前建议重新执行并保存截图或日志。

建议检查命令：

```bash
jps
hdfs dfsadmin -report
yarn node -list
spark-submit --master yarn --deploy-mode client \
  --class org.apache.spark.examples.SparkPi \
  /opt/bigdata/spark/examples/jars/spark-examples_2.12-3.5.1.jar 10
```

需要确认：

- master 上存在 NameNode、ResourceManager 等进程。
- worker 节点存在 DataNode、NodeManager。
- HDFS 能看到 DataNode。
- YARN 能看到 NodeManager。
- SparkPi 能在 YARN 上成功运行。

### 2.3 整理演示命令

当前高置信阈值下重复簇数量较少，原 README 中的部分演示命令不一定适合展示重复簇效果。

建议最终演示命令使用：

```bash
bash scripts/07_query_demo.sh --top-clusters --limit 10
bash scripts/07_query_demo.sh --keyword "analytic function" --limit 10
bash scripts/07_query_demo.sh --keyword "group by" --limit 10
bash scripts/07_query_demo.sh --question-id 28753859 --limit 10
```

不建议把下面命令作为重复簇核心演示：

```bash
bash scripts/07_query_demo.sh --question-id 470542
bash scripts/07_query_demo.sh --keyword "rownum order by" --limit 10
```

原因是当前最终结果中，这些查询不一定能命中重复簇。

## 3. 建议统一的文档内容

### 3.1 统一 JDK 版本说明

目前不同文档中存在版本差异：

- README 和实施方案中写 JDK 11。
- 完整实验报告中写 OpenJDK 8u412。

建议按最终服务器实际环境统一。如果最终确实使用 OpenJDK 8u412，则在 README、PPT 和实施文档中同步说明；如果最终使用 JDK 11，则修改实验报告对应表格。

### 3.2 统一 Spark 资源参数

当前 `conf/app.conf` 实际配置为：

```text
NUM_EXECUTORS=1
EXECUTOR_CORES=4
EXECUTOR_MEMORY=6g
SHUFFLE_PARTITIONS=96
```

但 PPT 大纲中写的是 4 个 executor、每个 2 cores、4g memory、shuffle partitions 为 24。建议以最终实际运行配置为准统一。

### 3.3 统一相似度阈值说明

当前 `conf/app.conf` 实际配置为：

```text
SIMILARITY_THRESHOLD=0.85
LSH_DISTANCE_THRESHOLD=0.15
MINHASH_TABLES=1
```

README、PPT 大纲和项目实施文档中多处提到 `similarity >= 0.65`。建议统一说明为：

- `0.65` 是推荐候选阈值。
- 本次最终实验为保证高置信和集群稳定，采用 `0.85`。
- 因此最终重复簇结果偏向高精度，召回较保守。

## 4. 建议优化

### 4.1 增加更多相似问题样例

当前导出的相似问题样例只有 2 对，重复簇样例只有 2 个簇。可以满足“非空演示”，但展示效果偏弱。

可选优化方向：

- 降低 `SIMILARITY_THRESHOLD` 到 `0.75` 或 `0.65`。
- 适当增加 `MINHASH_TABLES`。
- 记录不同阈值下的相似问题对数量和簇数量。
- 在报告中说明阈值对 precision/recall 的影响。

如果集群资源紧张，可以保留当前 `0.85` 结果，把“高精度、低召回”作为实验结论说明。

### 4.2 修正未使用参数

`src/cluster_lsh.py` 中定义了：

```text
--distance-threshold
```

但当前实现没有实际使用该参数，而是通过 MinHash bucket join 后计算 token Jaccard similarity，再按 `--similarity-threshold` 过滤。

建议二选一：

- 删除 `--distance-threshold` 参数，避免误导。
- 或改回使用 Spark ML `approxSimilarityJoin`，让 distance threshold 真正参与候选生成。

当前功能可以运行，因此这项不是必须项。

### 4.3 补充截图材料

建议为 PPT 或报告补充以下截图：

- HDFS NameNode UI。
- YARN ResourceManager UI。
- SparkPi 成功运行日志。
- Spark 作业运行记录。
- HDFS 输出目录。
- Demo 查询输出。

这些材料可以提高答辩可信度。

## 5. 提交材料检查表

提交前建议确认以下材料齐全：

- 源代码：`project-stackoverflow-clustering/src`
- 运行脚本：`project-stackoverflow-clustering/scripts`
- 配置文件：`project-stackoverflow-clustering/conf/app.conf`
- 使用说明：`project-stackoverflow-clustering/README.md`
- 项目文档：`project-stackoverflow-clustering/docs/项目实施文档.md`
- 完整实验报告：`project-stackoverflow-clustering/docs/完整实验报告.docx`
- PPT：`project-stackoverflow-clustering/docs/运维文本聚类项目汇报.pptx`
- 输出样例：`project-stackoverflow-clustering/output/hdfs_output`
- Demo 命令说明：README 或 PPT 中明确列出最终可用命令

## 6. 推荐收尾顺序

1. 确认服务器集群当前可用，并保存必要截图。
2. 统一 README、PPT、项目文档和实验报告中的版本与参数。
3. 制作正式 PPT。
4. 整理最终 Demo 命令，提前在服务器上跑一遍。
5. 如果时间充足，尝试降低阈值生成更多相似问题样例；如果时间不够，保留当前高置信结果并在报告中解释原因。

## 7. 本次收尾完成记录

- 已制作正式 PPT：`docs/运维文本聚类项目汇报.pptx`。
- 已统一 README、项目实施文档和 PPT 大纲中的 JDK 版本、Spark 资源参数、MinHash/相似度阈值和 Demo 命令。
- 已将 `src/cluster_lsh.py` 中的 `--distance-threshold` 接入过滤逻辑，避免配置参数未生效。
- 已连接服务器执行集群复核，日志保存于 `docs/verification/`。
- 已确认 master 上 NameNode、SecondaryNameNode、ResourceManager 存在，worker1/worker2 上 DataNode、NodeManager 存在。
- 已确认 HDFS report 中有 2 个 live datanodes，YARN node list 中有 2 个 RUNNING NodeManager。
- 已完成 SparkPi 小内存 YARN 复核，`sparkpi_small_retry_2026-05-27.log` 中记录 `Pi is roughly 3.142279142279142` 和 `SPARK_PI_SMALL_EXIT=0`。
- 已复核推荐 Demo 命令并保存日志。针对关键词查询慢的问题，已优化 `query_demo.py` 为按模式懒加载并优先过滤多文档簇；优化后 `--keyword "analytic function"` 与 `--top-clusters` 均完整返回。
