运维文本聚类项目 - 创新点叙事与答辩话术
============================================

生成日期：2026-06-16
状态标记：本文档列出的四个创新点，我们会全部在 v2 分支实现。
说明：本文档是答辩讲稿原料。每个创新点都按统一结构组织，方便直接抄进 PPT 和讲稿。


------------------------------------------------------------
总叙事（PPT 创新点页用这一段串起来）
------------------------------------------------------------

我们没有发明新算法，但把课程项目做出了工业界态度。

1. 拒绝伪造指标 —— 三层诚实评测，没人工标签就显式声明 pending
2. 真实工程踩坑 —— stage 9 长尾 29 分钟超时 → 标签分块 LSH
3. 领域知识融入算法 —— ORA 错误码作为 Oracle 独有的强信号
4. 算法到业务的最后一公里 —— 主题 × 去重双层视图

这四件事的共同点是：都来自我们真实做项目时遇到的问题，不是从论文里抄的关键词。

这个总叙事的杀伤力在于：它对冲了创新点本身不够"硬核"的问题。老师听完会觉得"这组学生有研究态度"，比"我们用了 BERT"更稳。


============================================================
创新点 1：拒绝伪造指标的"三层诚实评测框架"
============================================================
状态：会做（在 v2 分支实现）

【一句话定义】
我们没有像大多数学生项目那样直接编一个 F1=0.92，而是把评测拆成"原始数据核验
→ 参数扫描 → 人工审核"三层，没人工标签时显式声明 precision = null，杜绝刷分。

【发现的问题】
StackOverflow 重复问题去重没有现成的 ground truth，行业里很多论文和课程项目都用
"自己给自己打分"的方式糊弄过去——挑几个看起来像的对，宣布 F1 多少。我们意识到，
没有人工标签的情况下，任何 precision/recall/F1 都是自欺欺人。

【我们的做法】
三层评测体系：

  L1 原始数据核验：
    - 文件：validate_raw_data.py → output/validation/raw_data_validation.json
    - 输出：文件 SHA256、问题数、回答数完全可复现
    - 是否依赖人工：否

  L2 参数扫描：
    - 文件：parameter_sweep_summary.csv（5 组真实运行）
    - 输出：候选对数、簇数、平均相似度、耗时
    - 是否依赖人工：否

  L3 人工审核：
    - 文件：evaluation/review_candidates.csv + review_metrics.json
    - 输出：真实 precision，未标注时 status = pending_labels
    - 是否依赖人工：是

我们计划在答辩前真实标注 50 条候选，让 L3 从 pending_labels 变成有真数字的
precision，并在 PPT 上明示"50 条标注样本，precision=X，置信区间宽，不外推"。

【可量化证据】
- 原始数据核验输出 SHA256 = 799f1a5...861d，文件大小 588,919,290 bytes ——
  任何人重新跑都是同一个数。
- 参数扫描产出 7 行真实运行记录，包含 YARN MinHashLSH（0.65/0.75/0.85）、
  Spark local、本地兜底四类方法对照。
- 人工审核 50 条后给出 precision 实际值（实现时填）。

【边界与诚实声明】
- 50 条标签不能外推到 152,758 全量。
- recall 没有 ground truth 上限，所以 F1 算不出，我们不算。
- 这是一个评测方法论，不是一个新算法。

【为什么这是创新】
教科书的标准做法是"算个 F1 写在论文里"。我们反过来——老老实实承认算不出，把
"承认算不出"做成产品化的状态字段（pending_labels）。这件事态度本身比技术更难，
老师课堂上听过太多假 F1。

【答辩 30 秒话术】
"我们不写 F1=0.92。原因是 152K 问题没有 ground truth，任何宣称的 F1 都是自评。
我们改用三层评测：原始数据可复现核验、参数扫描真实跑、人工标 50 条得到一个有
置信区间的 precision。这套框架本身可以套用到任何缺乏标签的文本聚类项目。"


============================================================
创新点 2：标签分块 LSH（解决 stage 9 长尾）
============================================================
状态：会做（在 v2 分支实现）

【一句话定义】
我们发现 Spark 官方 approxSimilarityJoin 在 152K 文档全局比对时出现 stage 9
长尾（29 分钟未完成被迫中止），用按 Oracle 主标签先分桶、桶内做 LSH 的方式把
全局 join 拆成若干小 join，把不可完成的任务变成可完成。

【发现的问题】
这是个真实踩过的坑，不是编出来的（看 docs/verification/full_approx075_2026-06-14.md）：
- 配置：YARN, similarity=0.75, numHashTables=4, approxSimilarityJoin
- 现象：stage 9 长尾，executor 心跳超时，29 分钟后主动 kill
- 根因：MinHash 桶里有些 hash bucket 命中过万文档，self-join 产生 N² 候选对爆炸

【我们的做法】
按 tags[0] 把数据切成若干主题分区，每个分区独立做 LSH，最后把所有分区的相似对 union：

  全局 LSH（152K × 152K → stage 9 长尾）
      ↓
  sql 桶（69K）+ plsql 桶（22K）+ java 桶（10K）+ 其他桶
  每桶独立 MinHashLSH，桶内 self-join
  最后 union 所有桶的相似对

边界处理：
- 一个问题可能有多个 tag，我们用主标签（第一个 tag，或频次最高的非
  oracle-database tag）保证不重复计算
- 跨标签的相似问题（罕见）会漏掉——这是 precision/recall 的显式取舍，写进 PPT

【可量化证据】
- 改造前：full_approx075_2026-06-14.log 记录 29 分钟未完成
- 改造后：（实现后填）跑通时间、相似对数、是否覆盖原 0.75 baseline 的 2 对
- 子 LSH 单桶最大 size：sql 桶约 69K，依然是数据倾斜最大的桶——我们要展示它的
  耗时也比全局 join 短

【边界与诚实声明】
- 牺牲了跨标签的潜在重复对（例如一个 sql tag 的问题和一个 plsql tag 的问题
  语义上重复）
- 这是经典的"分治换可扩展性"取舍，不是免费午餐
- 这个做法在我们的实施方案文档第 14.2 节其实有提示，但是我们实际把它实现并
  量化了

【为什么这是创新】
不是算法发明，是工程问题驱动的架构调整。老师爱听这类故事："我们不是因为论文
里这样说才这样做，是因为我们的集群 29 分钟跑不出来才被迫这样做"。这种基于真
实痛点的工程取舍，比堆 fancy 算法更有说服力。

【答辩 30 秒话术】
"我们最早用 Spark 官方 approxSimilarityJoin 跑全量，stage 9 长尾 29 分钟未完
成。原因是有些 MinHash 桶命中过万文档，self-join 候选爆炸。我们做了个工程取舍——
按 Oracle 主标签先分块、桶内独立 LSH，把一个跑不动的全局任务拆成若干个小任务。
代价是丢掉跨标签的少量重复对，收益是任务可完成且可解释。"


============================================================
创新点 3：ORA 错误码作为领域强信号
============================================================
状态：会做（在 v2 分支实现）

【一句话定义】
我们注意到 Oracle 问答中 ORA-XXXXX 错误码是强语义信号——两个问题都提
ORA-04031 几乎一定是同类问题，但通用 TF-IDF 把它当作普通 token 处理。我们把
ORA 码独立抽取成结构化特征，加入相似度计算。

【发现的问题】
- TF-IDF 给 ORA-04031 和 select 的权重差异只来自文档频率，没有领域语义。
- 但实际上：两个问题都包含 ORA-04031（共享池内存不足），即使标题、正文措辞
  完全不同，也几乎肯定是同一个运维问题。
- 通用算法看不到这种领域强 prior。

【我们的做法】
1. 在 preprocess 阶段用正则 \bORA-\d{4,5}\b 抽取 ora_codes 数组列
2. 在 LSH 出相似对后，加一条领域加分规则：
     if src.ora_codes ∩ dst.ora_codes ≠ ∅:
         similarity_boosted = min(1.0, similarity + 0.10)
         ora_match = True
3. 在 demo 里专门做一个"ORA 码驱动的重复簇"视图，展示原本 similarity = 0.62
   （在 0.65 阈值下被过滤）但因为共享 ORA-04031 被拉回的对

【可量化证据】（实现后填）
- 全量数据中包含 ORA 码的问题数：N 个
- ORA 码 Top 20 分布
- ORA 加分前后相似对数量变化、新增的高质量对样例
- 在人工审核 50 条中，ORA 加分召回的对 precision 是否高于平均

【边界与诚实声明】
- ORA 加分系数 0.10 是经验值，不是 learned，需要在 PPT 里明说
- 不同 ORA 码语义距离不同（ORA-00001 unique constraint 和 ORA-00904 invalid
  identifier 是不同范畴），我们当前一视同仁——这是简化
- 这个做法只适用于 Oracle 这个领域，不是通用方法

【为什么这是创新】
领域知识 × 通用算法。课程项目大多直接套通用 TF-IDF + LSH，不会回头看"这个
数据集有什么独特结构可以利用"。我们看到了 ORA 码这个 Oracle 问答独有的强信号，
并把它工程化进了算法。这种"看见数据的独特性"是数据科学家的核心素养。

【答辩 30 秒话术】
"我们发现 Oracle 问答里有个独特的强信号——ORA 错误码。两个问题都提 ORA-04031
几乎一定是同一类运维问题。但 TF-IDF 看不到这一点。我们把 ORA 码独立抽成结构
化字段，相似度计算时加 0.1 的领域加分。这是一个通用算法 + 领域知识的小例子。
在 50 条人工审核样本中，ORA 加分召回的对 precision 比平均高 X%。"（X 实测填）


============================================================
创新点 4：主题聚类 × 重复检测的双层互补视图
============================================================
状态：会做（在 v2 分支实现）

【一句话定义】
我们同时跑了主题聚类（BisectingKMeans，回答"数据分布"）和重复检测
（MinHashLSH，回答"问题去重"）两条独立链路，并在 demo 里做成双层叠加视图——
展示每个主题里包含多少重复簇、去重后剩多少独立问题。

【发现的问题】
- 单独看主题聚类：能讲"Oracle 问题分 50 个主题"，但回答不了"重复有多少"
- 单独看 LSH 去重：能讲"找出 16 对重复"，但回答不了"重复集中在哪类问题"
- 两者结合才能回答实际工程问题："我们的客服 Oracle 问题库主要分什么主题？
  哪些主题重复率高？去重后能压缩多少？"

【我们的做法】
在现有 topic_clusters 和 clusters 输出基础上，做一个 join 视图：

  对每个 topic_id：
    - topic_size: 该主题下问题数
    - duplicate_cluster_count: 该主题内 LSH 找出的多文档簇数
    - duplicate_question_count: 该主题内被合并的重复问题数
    - dedup_ratio: duplicate_question_count / topic_size
    - top_terms: 主题关键词
    - sample_duplicate_pair: 一个代表性的重复对

输出到 demo/index.html 一个新 tab："主题 × 去重交叉视图"。

【可量化证据】（实现后填）
- 50 个主题里，重复率最高的主题及其 top terms
- 去重前后总问题数变化
- 重复率分布（是均匀还是集中在少数主题）

【边界与诚实声明】
- 主题聚类的 K=50 是经验选的，不是严格调参
- 主题边界本身有模糊性，不是 ground truth
- 这是一个展示性增强，不是新算法

【为什么这是创新】
教科书做法是把两个算法各跑一次、各报一组数。我们做的是算法结果的产品化整合——
把"算法输出"变成"业务可读的运维洞察"。这一步在工业界叫做 operationalization，
是数据从科研走向落地的关键一公里。

【答辩 30 秒话术】
"主题聚类回答'数据分布'，重复检测回答'去重'，单独看都不完整。我们把两个结果
join 起来，得到一个'主题 × 去重交叉视图'：能直接告诉运维团队，哪类问题重复率
最高、去重后能压缩多少。这一步把'算法结果'变成'业务洞察'。"


============================================================
实现顺序（v2 分支）
============================================================

1. 先做 D3 ORA 码特征（半天）
   - 改 src/preprocess.py 加 ora_codes 列
   - 改 src/cluster_lsh.py 加 boost 规则
   - 产出 ORA 加分前后对照

2. 再做 D2 标签分块 LSH（一天）
   - 新写 src/cluster_lsh_partitioned.py，按 tags[0] 分桶
   - 产出耗时与相似对对比

3. 接着做 D4 双层视图（半天）
   - 新写 src/build_topic_dedup_view.py，输出 join 表
   - 改 demo/index.html 加 tab

4. 最后做 D1 评测层（半天）
   - 真标注 50 条 review_candidates.csv（写一个交互辅助脚本帮快速标）
   - 跑 src/evaluate_review_labels.py，更新 review_metrics.json
   - 改 PPT 文案，把假 F1 替换成"50 条 precision = X"

总计约 2.5 个工作日。
