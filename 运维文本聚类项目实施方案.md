# 运维文本聚类项目实施方案

## 1. 项目背景

本项目对应 `大数据分析技术项目-2026版.pdf` 中的第二个项目。

项目名称：运维文本聚类

数据来源：StackOverflow

项目目标：将问答对进行聚类，用于问题去重，将本质上相同但表达不同的问题聚到一起。

课程要求：

- 使用集群搭建实验环境
- 使用 Hadoop 和 Spark
- 完成 15-30 分钟项目报告和系统演示
- 提交源代码、使用说明、PPT 和项目文档

数据目录：

```text
/Users/zhiqizhang/development/BigData/StackOverFlow_Oracle_Database
```

主要数据文件：

```text
oracle_database_questions.json
数据说明.md
```

数据规模：

```text
JSON 文件大小：约 589 MB
问题数：152758
回答数：203393
问题评论数：331642
回答评论数：220296
总文本对象数：908089
```

JSON 是一个数组，每个元素是一个 StackOverflow 问题，结构示例：

```json
{
  "question_id": "问题 id",
  "title": "问题标题",
  "body": "问题正文 HTML",
  "tags": ["sql", "oracle-database"],
  "score": 134,
  "comments": [
    {
      "body": "评论",
      "score": 1
    }
  ],
  "answers": [
    {
      "answer_id": "回答 id",
      "body": "回答正文 HTML",
      "score": 56,
      "comments": [
        {
          "body": "评论",
          "score": 0
        }
      ]
    }
  ]
}
```

## 2. 服务器信息

三台服务器信息如下：

```text
Master:  10.176.62.239
Worker1: 10.176.62.240
Worker2: 10.176.62.241

用户名：root
密码：Fdwangp123
```

建议主机名：

```text
10.176.62.239 master
10.176.62.240 worker1
10.176.62.241 worker2
```

## 3. 总体架构

使用 Hadoop HDFS 存储数据，使用 YARN 做资源调度，使用 Spark on YARN 做分布式文本处理、特征工程、相似度计算和聚类。

推荐组件版本：

```text
JDK: Java 11
Hadoop: 3.3.6
Spark: 3.5.1, pre-built for Hadoop 3
Python: 3.8+
```

部署路径建议：

```text
/opt/bigdata/hadoop
/opt/bigdata/spark
/opt/bigdata/project-stackoverflow-clustering
/data/hadoop/name
/data/hadoop/data
```

HDFS 目录规划：

```text
/user/bigdata/stackoverflow/raw/oracle_database_questions.json
/user/bigdata/stackoverflow/jsonl/questions.jsonl
/user/bigdata/stackoverflow/parquet/questions
/user/bigdata/stackoverflow/parquet/features
/user/bigdata/stackoverflow/output/eda
/user/bigdata/stackoverflow/output/clusters
/user/bigdata/stackoverflow/output/similar_pairs
/user/bigdata/stackoverflow/output/demo_index
```

整体流程：

```text
原始 JSON
  -> JSON Lines 转换
  -> 上传 HDFS
  -> Spark 读取和清洗
  -> 构造问答文本
  -> 特征工程
  -> 主题聚类
  -> LSH 相似问题发现
  -> 连通分量生成重复问题簇
  -> Demo 展示
```

## 4. 第一阶段：服务器初始化

在三台机器上分别设置主机名。

10.176.62.239：

```bash
hostnamectl set-hostname master
```

10.176.62.240：

```bash
hostnamectl set-hostname worker1
```

10.176.62.241：

```bash
hostnamectl set-hostname worker2
```

三台机器都配置 `/etc/hosts`：

```bash
cat >> /etc/hosts <<'EOF'
10.176.62.239 master
10.176.62.240 worker1
10.176.62.241 worker2
EOF
```

安装基础工具。

Ubuntu/Debian：

```bash
apt update
apt install -y openjdk-11-jdk ssh rsync wget curl vim net-tools python3 python3-pip
systemctl enable ssh
systemctl start ssh
```

CentOS/RHEL：

```bash
yum install -y java-11-openjdk-devel openssh-server openssh-clients rsync wget curl vim net-tools python3 python3-pip
systemctl enable sshd
systemctl start sshd
```

确认 Java：

```bash
java -version
```

## 5. 第二阶段：配置 SSH 免密

以下命令只在 master 上执行。

```bash
ssh-keygen -t rsa -b 4096 -N "" -f ~/.ssh/id_rsa
```

将公钥复制到三台机器：

```bash
ssh-copy-id root@master
ssh-copy-id root@worker1
ssh-copy-id root@worker2
```

如果提示输入密码，输入：

```text
Fdwangp123
```

验证：

```bash
ssh master "hostname"
ssh worker1 "hostname"
ssh worker2 "hostname"
```

成功标准：以上命令均不再要求密码，并能分别输出 `master`、`worker1`、`worker2`。

## 6. 第三阶段：安装 Hadoop

只在 master 上下载和解压，然后分发到 worker。

```bash
mkdir -p /opt/bigdata
cd /opt/bigdata

wget https://archive.apache.org/dist/hadoop/common/hadoop-3.3.6/hadoop-3.3.6.tar.gz
tar -xzf hadoop-3.3.6.tar.gz
ln -s hadoop-3.3.6 hadoop
```

三台机器都需要配置环境变量 `/etc/profile.d/bigdata.sh`：

```bash
cat > /etc/profile.d/bigdata.sh <<'EOF'
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
export HADOOP_HOME=/opt/bigdata/hadoop
export HADOOP_CONF_DIR=$HADOOP_HOME/etc/hadoop
export SPARK_HOME=/opt/bigdata/spark
export PATH=$JAVA_HOME/bin:$HADOOP_HOME/bin:$HADOOP_HOME/sbin:$SPARK_HOME/bin:$SPARK_HOME/sbin:$PATH
export PYSPARK_PYTHON=python3
EOF

source /etc/profile.d/bigdata.sh
```

如果是 CentOS，`JAVA_HOME` 可能是：

```text
/usr/lib/jvm/java-11-openjdk
```

可用下面命令确认：

```bash
readlink -f $(which java)
```

### 6.1 配置 Hadoop workers

只在 master 上编辑。

文件：

```text
/opt/bigdata/hadoop/etc/hadoop/workers
```

内容：

```text
worker1
worker2
```

### 6.2 配置 core-site.xml

文件：

```text
/opt/bigdata/hadoop/etc/hadoop/core-site.xml
```

内容：

```xml
<configuration>
  <property>
    <name>fs.defaultFS</name>
    <value>hdfs://master:9000</value>
  </property>
</configuration>
```

### 6.3 配置 hdfs-site.xml

文件：

```text
/opt/bigdata/hadoop/etc/hadoop/hdfs-site.xml
```

内容：

```xml
<configuration>
  <property>
    <name>dfs.namenode.name.dir</name>
    <value>file:/data/hadoop/name</value>
  </property>
  <property>
    <name>dfs.datanode.data.dir</name>
    <value>file:/data/hadoop/data</value>
  </property>
  <property>
    <name>dfs.replication</name>
    <value>2</value>
  </property>
</configuration>
```

### 6.4 配置 yarn-site.xml

文件：

```text
/opt/bigdata/hadoop/etc/hadoop/yarn-site.xml
```

内容：

```xml
<configuration>
  <property>
    <name>yarn.resourcemanager.hostname</name>
    <value>master</value>
  </property>

  <property>
    <name>yarn.nodemanager.aux-services</name>
    <value>mapreduce_shuffle</value>
  </property>

  <property>
    <name>yarn.nodemanager.resource.memory-mb</name>
    <value>8192</value>
  </property>

  <property>
    <name>yarn.scheduler.maximum-allocation-mb</name>
    <value>8192</value>
  </property>
</configuration>
```

### 6.5 配置 mapred-site.xml

文件：

```text
/opt/bigdata/hadoop/etc/hadoop/mapred-site.xml
```

内容：

```xml
<configuration>
  <property>
    <name>mapreduce.framework.name</name>
    <value>yarn</value>
  </property>
</configuration>
```

### 6.6 配置 hadoop-env.sh

文件：

```text
/opt/bigdata/hadoop/etc/hadoop/hadoop-env.sh
```

追加：

```bash
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
```

如果实际 `JAVA_HOME` 不同，需要按服务器实际路径调整。

### 6.7 创建 Hadoop 数据目录

master：

```bash
mkdir -p /data/hadoop/name /data/hadoop/data
```

worker：

```bash
ssh worker1 "mkdir -p /data/hadoop/data"
ssh worker2 "mkdir -p /data/hadoop/data"
```

### 6.8 分发 Hadoop

在 master 上执行：

```bash
rsync -av /opt/bigdata/hadoop-3.3.6 worker1:/opt/bigdata/
rsync -av /opt/bigdata/hadoop-3.3.6 worker2:/opt/bigdata/

ssh worker1 "ln -sfn /opt/bigdata/hadoop-3.3.6 /opt/bigdata/hadoop"
ssh worker2 "ln -sfn /opt/bigdata/hadoop-3.3.6 /opt/bigdata/hadoop"

scp /etc/profile.d/bigdata.sh worker1:/etc/profile.d/bigdata.sh
scp /etc/profile.d/bigdata.sh worker2:/etc/profile.d/bigdata.sh
```

### 6.9 格式化并启动 HDFS/YARN

只在 master 第一次执行格式化：

```bash
hdfs namenode -format
```

启动 Hadoop：

```bash
start-dfs.sh
start-yarn.sh
```

验证：

```bash
jps
hdfs dfsadmin -report
yarn node -list
```

Web UI：

```text
HDFS NameNode: http://10.176.62.239:9870
YARN:          http://10.176.62.239:8088
```

成功标准：

- master 上能看到 NameNode、ResourceManager
- worker 上能看到 DataNode、NodeManager
- `hdfs dfsadmin -report` 能看到 2 个 DataNode
- `yarn node -list` 能看到 2 个 NodeManager

## 7. 第四阶段：安装 Spark

只在 master 下载，然后分发。

```bash
cd /opt/bigdata
wget https://archive.apache.org/dist/spark/spark-3.5.1/spark-3.5.1-bin-hadoop3.tgz
tar -xzf spark-3.5.1-bin-hadoop3.tgz
ln -s spark-3.5.1-bin-hadoop3 spark
```

配置 Spark：

```bash
cp /opt/bigdata/spark/conf/spark-env.sh.template /opt/bigdata/spark/conf/spark-env.sh
cat >> /opt/bigdata/spark/conf/spark-env.sh <<'EOF'
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
export HADOOP_CONF_DIR=/opt/bigdata/hadoop/etc/hadoop
export YARN_CONF_DIR=/opt/bigdata/hadoop/etc/hadoop
export SPARK_MASTER_HOST=master
export PYSPARK_PYTHON=python3
EOF
```

文件：

```text
/opt/bigdata/spark/conf/spark-defaults.conf
```

内容：

```text
spark.master                     yarn
spark.eventLog.enabled           true
spark.eventLog.dir               hdfs:///spark-history
spark.serializer                 org.apache.spark.serializer.KryoSerializer
spark.sql.shuffle.partitions     24
spark.default.parallelism        24
```

分发 Spark：

```bash
rsync -av /opt/bigdata/spark-3.5.1-bin-hadoop3 worker1:/opt/bigdata/
rsync -av /opt/bigdata/spark-3.5.1-bin-hadoop3 worker2:/opt/bigdata/

ssh worker1 "ln -sfn /opt/bigdata/spark-3.5.1-bin-hadoop3 /opt/bigdata/spark"
ssh worker2 "ln -sfn /opt/bigdata/spark-3.5.1-bin-hadoop3 /opt/bigdata/spark"
```

创建 Spark history 目录：

```bash
hdfs dfs -mkdir -p /spark-history
```

测试 Spark on YARN：

```bash
spark-submit \
  --master yarn \
  --deploy-mode client \
  --class org.apache.spark.examples.SparkPi \
  /opt/bigdata/spark/examples/jars/spark-examples_2.12-3.5.1.jar \
  10
```

成功标准：输出近似 `Pi is roughly 3.14`，并且 YARN UI 可以看到 Spark 任务。

## 8. 第五阶段：项目代码目录设计

在 master 上创建项目目录：

```bash
mkdir -p /opt/bigdata/project-stackoverflow-clustering/{scripts,src,conf,docs,output,data}
cd /opt/bigdata/project-stackoverflow-clustering
```

建议目录结构：

```text
project-stackoverflow-clustering/
  README.md
  conf/
    app.conf
  scripts/
    00_upload_raw.sh
    01_json_to_jsonl.sh
    02_prepare_hdfs.sh
    03_submit_eda.sh
    04_submit_preprocess.sh
    05_submit_cluster.sh
    06_export_demo.sh
  src/
    json_to_jsonl.py
    eda.py
    preprocess.py
    feature_engineering.py
    cluster_lsh.py
    connected_components.py
    query_demo.py
  docs/
    项目实施文档.md
    PPT大纲.md
  output/
  data/
```

## 9. 第六阶段：上传数据到 HDFS

先把本地数据拷贝到 master。

在本机执行：

```bash
scp -r /Users/zhiqizhang/development/BigData/StackOverFlow_Oracle_Database \
  root@10.176.62.239:/opt/bigdata/project-stackoverflow-clustering/data
```

在 master 上执行：

```bash
hdfs dfs -mkdir -p /user/bigdata/stackoverflow/raw
hdfs dfs -put -f /opt/bigdata/project-stackoverflow-clustering/data/StackOverFlow_Oracle_Database/oracle_database_questions.json \
  /user/bigdata/stackoverflow/raw/
```

验证：

```bash
hdfs dfs -ls -h /user/bigdata/stackoverflow/raw
```

## 10. 第七阶段：JSON 数组转 JSON Lines

原始 JSON 是一个大数组，不利于 Spark 分布式读取。建议先转为 JSONL，每行一个 question。

在 master 上安装流式 JSON 解析库：

```bash
pip3 install ijson
```

创建 `src/json_to_jsonl.py`：

```python
import json
import ijson
import sys

input_path = sys.argv[1]
output_path = sys.argv[2]

with open(input_path, "rb") as fin, open(output_path, "w", encoding="utf-8") as fout:
    for item in ijson.items(fin, "item"):
        fout.write(json.dumps(item, ensure_ascii=False) + "\n")
```

执行转换：

```bash
python3 src/json_to_jsonl.py \
  /opt/bigdata/project-stackoverflow-clustering/data/StackOverFlow_Oracle_Database/oracle_database_questions.json \
  /opt/bigdata/project-stackoverflow-clustering/output/questions.jsonl
```

上传 JSONL：

```bash
hdfs dfs -mkdir -p /user/bigdata/stackoverflow/jsonl
hdfs dfs -put -f /opt/bigdata/project-stackoverflow-clustering/output/questions.jsonl \
  /user/bigdata/stackoverflow/jsonl/
```

验证行数：

```bash
wc -l /opt/bigdata/project-stackoverflow-clustering/output/questions.jsonl
```

预期输出：

```text
152758
```

## 11. 第八阶段：Spark 数据预处理

目标：把嵌套 JSON 转成结构化 Parquet，并构造用于聚类的问答文档。

文档构造策略：

```text
doc_id = question_id
title_text = title
question_text = body 去 HTML 后文本
tag_text = tags 拼接
answer_text = 按 score 降序选前 3 个回答，拼接 body
comment_text = 可选，仅用于扩展，不建议默认加入聚类特征
doc_text = title 重复 3 次 + question_text 重复 2 次 + tag_text + top_answer_text
```

设计理由：

- 去重目标主要是“问题”去重，因此 title 和 question body 权重大
- 回答能补充语义，但可能引入噪声，只取高分回答前 3 个
- comments 噪声较多，默认只做 EDA，不进入主聚类特征

创建 `src/preprocess.py`：

```python
from pyspark.sql import SparkSession
from pyspark.sql.functions import col, expr, lower, regexp_replace, concat_ws, array_join

spark = SparkSession.builder.appName("stackoverflow-preprocess").getOrCreate()

input_path = "hdfs:///user/bigdata/stackoverflow/jsonl/questions.jsonl"
output_path = "hdfs:///user/bigdata/stackoverflow/parquet/questions"

df = spark.read.json(input_path)

df = df.withColumn("tags_text", array_join(col("tags"), " "))

df = df.withColumn(
    "top_answers",
    expr("""
      transform(
        slice(
          reverse(array_sort(transform(answers, x -> named_struct('score', x.score, 'body', x.body)))),
          1,
          3
        ),
        x -> x.body
      )
    """)
)

df = df.withColumn("answers_text", array_join(col("top_answers"), " "))

df = df.withColumn(
    "raw_doc_text",
    concat_ws(
        " ",
        col("title"), col("title"), col("title"),
        col("body"), col("body"),
        col("tags_text"),
        col("answers_text")
    )
)

clean = lower(col("raw_doc_text"))
clean = regexp_replace(clean, "<pre><code>[\\s\\S]*?</code></pre>", " codeblock ")
clean = regexp_replace(clean, "<[^>]+>", " ")
clean = regexp_replace(clean, "&[a-z]+;", " ")
clean = regexp_replace(clean, "[^a-z0-9_#.+\\-/ ]", " ")
clean = regexp_replace(clean, "\\s+", " ")

out = df.select(
    col("question_id").cast("string").alias("doc_id"),
    col("question_id").cast("string"),
    col("title"),
    col("body"),
    col("tags"),
    col("score").cast("int"),
    expr("size(answers)").alias("answer_count"),
    expr("size(comments)").alias("comment_count"),
    clean.alias("clean_text")
)

out.repartition(24).write.mode("overwrite").parquet(output_path)

spark.stop()
```

提交任务：

```bash
spark-submit \
  --master yarn \
  --deploy-mode client \
  --driver-memory 2g \
  --executor-memory 3g \
  --executor-cores 2 \
  --num-executors 4 \
  src/preprocess.py
```

验证：

```bash
hdfs dfs -ls /user/bigdata/stackoverflow/parquet/questions
```

## 12. 第九阶段：EDA 分析

EDA 必须产出以下统计，作为报告内容：

- 总问题数、回答数、评论数
- 每个问题平均回答数、最大回答数、无回答问题数量
- Top 30 标签
- 问题 score 分布
- 回答数和 score 的关系
- 文本长度分布
- 高频词统计
- 最常见 Oracle 错误码，例如 ORA-xxxxx

已知基础统计可直接写入报告：

```text
问题数：152758
回答数：203393
问题评论数：331642
回答评论数：220296
平均每问题回答数：1.331
最大回答数：34
无回答问题数：29937
最高分问题：question_id=470542
最高分问题标题：How do I limit the number of rows returned by an Oracle query after ordering?
```

Top 标签：

```text
oracle-database: 152758
sql: 69761
plsql: 22721
oracle11g: 12287
java: 10796
database: 8740
c#: 5025
oracle-sqldeveloper: 4758
oracle10g: 4381
stored-procedures: 3976
jdbc: 3737
oracle-apex: 3584
oracle12c: 2763
sql-server: 2725
sqlplus: 2565
triggers: 2529
hibernate: 2522
mysql: 2373
php: 2331
join: 2118
```

## 13. 第十阶段：特征工程

使用 Spark ML Pipeline。

推荐主特征：

```text
RegexTokenizer
StopWordsRemover
HashingTF 或 CountVectorizer
IDF
Normalizer
```

Token 规则建议：

需要保留：

- sql、plsql、java、python、c#、jdbc 等技术词
- ora-xxxxx 错误码
- oracle11g、oracle12c 等版本词
- select、join、group-by、procedure、trigger、index 等数据库术语
- 代码相关 token

需要去除：

- 英文停用词
- HTML 残留
- StackOverflow 常见无意义词
- oracle-database，因为所有文档都有该标签，区分度低

领域停用词建议：

```python
domain_stopwords = [
    "oracle", "database", "oracle-database", "question", "answer",
    "thanks", "please", "help", "using", "use", "want", "need",
    "problem", "issue", "error", "codeblock"
]
```

注意：不要删除 `ora-xxxxx`，它对问题去重非常关键。

## 14. 第十一阶段：聚类算法路线

建议做两类结果，报告会更完整。

### 14.1 主题聚类

用途：展示 Oracle 问题主题分布。

算法：

```text
TF-IDF 向量 + BisectingKMeans
```

参数：

```text
k 候选：30, 50, 80, 100
推荐最终 k：50 或 80
评价指标：silhouette、簇大小分布、Top terms 人工可解释性
```

输出字段：

```text
cluster_id
doc_id
title
score
tags
cluster_top_terms
```

### 14.2 重复问题发现

用途：实现“问题去重”。

算法：

```text
TF-IDF/Token Set + MinHashLSH 或 BucketedRandomProjectionLSH
相似问题 pair 生成
基于相似边做连通分量，得到 duplicate clusters
```

推荐方案：

```text
1. tokens -> HashingTF(binary=True) -> MinHashLSH
2. approximateSimilarityJoin 找 Jaccard 距离小于 threshold 的 pair
3. similarity = 1 - distCol
4. 过滤 similarity >= 0.65
5. 对相似 pair 构图
6. 用 connected components 合并成重复问题簇
```

阈值建议：

```text
宽松候选：similarity >= 0.55
推荐展示：similarity >= 0.65
高置信去重：similarity >= 0.75
```

如果 pair 太多：

- 先按标签粗分区，例如 sql、plsql、java、jdbc
- 每个分区内做 LSH
- 限制每个问题最多保留 top 10 相似问题

## 15. 第十二阶段：Spark LSH 去重实现

创建 `src/cluster_lsh.py`：

```python
from pyspark.sql import SparkSession
from pyspark.sql.functions import col, lit
from pyspark.ml.feature import RegexTokenizer, StopWordsRemover, HashingTF, MinHashLSH
from pyspark.ml import Pipeline

spark = SparkSession.builder.appName("stackoverflow-lsh-clustering").getOrCreate()

df = spark.read.parquet("hdfs:///user/bigdata/stackoverflow/parquet/questions")
df = df.filter(col("clean_text").isNotNull())

tokenizer = RegexTokenizer(
    inputCol="clean_text",
    outputCol="raw_tokens",
    pattern=r"[^a-z0-9_#.+/-]+",
    gaps=True,
    minTokenLength=2
)

stopwords = StopWordsRemover.loadDefaultStopWords("english") + [
    "oracle", "database", "oracle-database", "question", "answer",
    "thanks", "please", "help", "using", "use", "want", "need",
    "problem", "issue", "codeblock"
]

remover = StopWordsRemover(
    inputCol="raw_tokens",
    outputCol="tokens",
    stopWords=stopwords
)

hashing = HashingTF(
    inputCol="tokens",
    outputCol="features",
    numFeatures=1 << 18,
    binary=True
)

pipeline = Pipeline(stages=[tokenizer, remover, hashing])
model = pipeline.fit(df)

features = model.transform(df).select(
    "doc_id",
    "title",
    "tags",
    "score",
    "tokens",
    "features"
)

lsh = MinHashLSH(
    inputCol="features",
    outputCol="hashes",
    numHashTables=8
)

lsh_model = lsh.fit(features)

pairs = lsh_model.approxSimilarityJoin(
    features.alias("a"),
    features.alias("b"),
    0.35,
    distCol="jaccard_distance"
)

pairs = pairs.select(
    col("a.doc_id").alias("src"),
    col("b.doc_id").alias("dst"),
    col("a.title").alias("src_title"),
    col("b.title").alias("dst_title"),
    (lit(1.0) - col("jaccard_distance")).alias("similarity")
).filter(col("src") < col("dst"))

pairs.write.mode("overwrite").parquet(
    "hdfs:///user/bigdata/stackoverflow/output/similar_pairs"
)

spark.stop()
```

提交命令：

```bash
spark-submit \
  --master yarn \
  --deploy-mode client \
  --driver-memory 4g \
  --executor-memory 4g \
  --executor-cores 2 \
  --num-executors 4 \
  src/cluster_lsh.py
```

验证：

```bash
hdfs dfs -ls /user/bigdata/stackoverflow/output/similar_pairs
```

## 16. 第十三阶段：连通分量聚类

简单实现策略：

```text
输入：similar_pairs(src, dst, similarity)
每个 doc 初始 cluster_id = doc_id
反复用边传播最小 cluster_id：
  src 和 dst 的 cluster_id 都更新为二者较小值
迭代 10-20 次或直到变化很小
输出 doc_id -> cluster_id
```

如果希望工程化，可以使用 GraphFrames，但需要额外 jar。课程项目中可以优先使用 Spark DataFrame 迭代实现，避免依赖问题。

聚类输出字段：

```text
cluster_id
cluster_size
doc_id
title
score
tags
representative_title
avg_similarity
```

代表问题选择规则：

- 同簇中 score 最高的问题作为 representative
- 若 score 相同，选择 title 较短的问题

去重展示示例：

```text
cluster_id = 12345
代表问题：How do I limit the number of rows returned by an Oracle query after ordering?
重复/相似问题：
  - Oracle rownum after order by ...
  - How to select top N rows in Oracle ...
  - Limit query result in Oracle SQL ...
```

## 17. 第十四阶段：Demo 设计

建议做一个轻量命令行 Demo，稳定且易演示。

功能：

- 输入 question_id，输出该问题所在聚类
- 输入关键词，搜索标题包含关键词的问题
- 展示同簇相似问题列表
- 展示 pair similarity
- 展示 Top clusters

示例命令：

```bash
spark-submit \
  --master yarn \
  --deploy-mode client \
  src/query_demo.py \
  --question-id 470542
```

输出示例：

```text
Query Question:
[470542] How do I limit the number of rows returned by an Oracle query after ordering?

Duplicate Cluster:
cluster_id: 470542
cluster_size: 18

Similar Questions:
1. [xxxx] Oracle SQL select top N rows after order by, similarity=0.82
2. [yyyy] How to limit rows in Oracle after sorting, similarity=0.78
3. [zzzz] ROWNUM and ORDER BY in Oracle, similarity=0.75
```

可选增强：用 Streamlit 做一个网页 Demo。若做 Streamlit，Spark 结果应先导出 CSV 或 Parquet 到本地，页面只读结果，避免现场跑大任务。

## 18. 第十五阶段：结果文件

最终 HDFS 输出：

```text
/user/bigdata/stackoverflow/output/eda/
/user/bigdata/stackoverflow/output/similar_pairs/
/user/bigdata/stackoverflow/output/clusters/
/user/bigdata/stackoverflow/output/demo_index/
```

导出到本地：

```bash
hdfs dfs -get -f /user/bigdata/stackoverflow/output \
  /opt/bigdata/project-stackoverflow-clustering/output/hdfs_output
```

需要提交的本地文件：

```text
README.md
src/*.py
scripts/*.sh
docs/项目实施文档.md
docs/PPT大纲.md
output/eda_summary.csv
output/top_tags.csv
output/cluster_samples.csv
output/similar_pair_samples.csv
```

## 19. 第十六阶段：报告/PPT 建议结构

PPT 15-30 分钟建议 12-15 页：

```text
1. 项目背景：StackOverflow Oracle 问题去重
2. 数据集介绍：字段、规模、来源
3. 集群环境：三台服务器、Hadoop、Spark、HDFS、YARN
4. 系统架构图：数据入湖 -> 预处理 -> 特征工程 -> LSH -> 聚类 -> Demo
5. 数据预处理：JSONL、HTML 清洗、问答文本构造
6. EDA 结果：问题数、回答数、Top 标签、score 分布
7. 文本特征：Tokenizer、StopWords、TF-IDF、HashingTF
8. 主题聚类：BisectingKMeans 方案和结果
9. 去重算法：MinHashLSH + 相似边 + 连通分量
10. 聚类结果展示：典型重复问题簇
11. 评价方法：人工抽样、相似度阈值、簇可解释性
12. 系统 Demo：输入 question_id 查相似问题
13. 性能与资源：Spark 任务耗时、分区数、executor 设置
14. 遇到的问题：JSON 大数组、HTML 噪声、同义表达
15. 总结与分工
```

## 20. 第十七阶段：验收标准

### 20.1 集群验收

```text
1. 三台机器 SSH 互通。
2. HDFS Web UI 能打开。
3. YARN Web UI 能打开。
4. spark-submit SparkPi 成功。
5. 数据成功上传到 HDFS。
```

### 20.2 数据处理验收

```text
1. Spark 能读取 JSONL 并写出 Parquet。
2. EDA 输出的问题数为 152758。
3. Top 标签结果包含 oracle-database、sql、plsql。
4. LSH 能产出 similar_pairs。
5. clusters 中存在 cluster_size > 1 的重复问题簇。
```

### 20.3 Demo 验收

```text
1. 输入 question_id 能展示原问题。
2. 能展示相似问题列表。
3. 能展示 similarity。
4. 能解释为什么这些问题被聚到一起。
```

## 21. 风险和处理

风险 1：Spark 直接读取原始 JSON 数组慢或失败。

处理：必须先转 JSONL，再上传 HDFS。

风险 2：HTML、代码块、链接噪声影响聚类。

处理：预处理时去 HTML，保留关键技术 token 和 ORA 错误码。

风险 3：LSH 阈值过低导致簇过大。

处理：从 similarity >= 0.75 开始展示高置信结果，再尝试 0.65。

风险 4：三台服务器内存较小。

处理：executor-memory 从 2g 或 3g 起步，shuffle partitions 设 24，必要时提高分区。

风险 5：GraphFrames 依赖复杂。

处理：优先用 Spark DataFrame 迭代实现连通分量，或只展示 similar_pairs 和小规模聚类样例。

## 22. 推荐执行顺序

```text
1. 初始化三台服务器
2. 配置 SSH 免密
3. 安装 Hadoop
4. 启动并验证 HDFS/YARN
5. 安装 Spark
6. 提交 SparkPi 验证 Spark on YARN
7. 拷贝数据到 master
8. 原始 JSON 转 JSONL
9. 上传 JSONL 到 HDFS
10. 运行 Spark 预处理，生成 Parquet
11. 运行 EDA，生成统计结果
12. 运行 LSH，生成 similar_pairs
13. 运行连通分量，生成 clusters
14. 准备 Demo 查询脚本
15. 整理 README、项目文档和 PPT
16. 进行现场演示
```

## 23. 最终结论

本项目的核心路线是：

```text
HDFS 存储原始和中间数据
Spark 做清洗、特征、LSH 相似度搜索和聚类
最后用典型重复问题簇做 Demo
```

另一个 AI 或实施者按本文档执行时，应先保证 Hadoop/Spark 集群可用，再处理 JSONL 转换和 HDFS 入湖，最后实现 `preprocess.py`、`cluster_lsh.py`、`connected_components.py` 和 `query_demo.py`。
